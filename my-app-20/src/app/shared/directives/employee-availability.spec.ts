import { EmployeeAvailability } from './employee-availability';

describe('EmployeeAvailability', () => {
  it('should create an instance', () => {
    const directive = new EmployeeAvailability();
    expect(directive).toBeTruthy();
  });
});
